import { equalsToFalse } from '../';
describe('equalsToFalse', function () {
  it('return true', function () {
    expect(equalsToFalse(false)).toBe(true);
  });
  it('return false', function () {
    expect(equalsToFalse(true)).toBe(false);
  });
});