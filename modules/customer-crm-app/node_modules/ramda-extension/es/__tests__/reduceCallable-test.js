import { reduceCallable } from '../';
describe('reduceCallable', function () {
  it('should return result after appling arguments ', function () {
    expect(reduceCallable(function (a) {
      return function (b) {
        return a + b;
      };
    }, [1, 2])).toBe(3);
  });
});