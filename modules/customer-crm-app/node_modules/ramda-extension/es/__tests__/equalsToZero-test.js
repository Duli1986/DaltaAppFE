import { equalsToZero } from '../';
describe('equalsToZero', function () {
  it('should be false', function () {
    expect(equalsToZero(1)).toBe(false);
  });
  it('should be true', function () {
    expect(equalsToZero(0)).toBe(true);
  });
});