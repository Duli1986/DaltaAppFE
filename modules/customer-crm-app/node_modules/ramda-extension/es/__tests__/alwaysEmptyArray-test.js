import { alwaysEmptyArray } from '../';
describe('alwaysEmptyArray', function () {
  it('returns []', function () {
    expect(alwaysEmptyArray()).toEqual([]);
  });
  it('always returns a new array', function () {
    expect(alwaysEmptyArray()).not.toBe(alwaysEmptyArray());
  });
});