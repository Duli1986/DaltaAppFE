import { uniqDropLastWhile } from '../';
describe('uniqDropLastWhile', function () {
  it('returns unique result of dropLastWhile', function () {
    expect(uniqDropLastWhile(function (x) {
      return x !== 4;
    }, [1, 1, 1, 2, 3, 4, 3, 2, 1])).toEqual([1, 2, 3, 4]);
  });
});