import { joinWithUnderscore } from '../';
describe('joinWithUnderscore', function () {
  it('join array of string with underscore determiner', function () {
    expect(joinWithUnderscore(['a', 'b', 'c'])).toEqual('a_b_c');
  });
});