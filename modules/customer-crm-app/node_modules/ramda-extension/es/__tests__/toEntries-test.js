import { toEntries } from '../';
describe('toEntries', function () {
  it('creates entries from object', function () {
    expect(toEntries({
      a: 'b',
      c: 'd'
    })).toEqual([{
      a: 'b'
    }, {
      c: 'd'
    }]);
  });
});