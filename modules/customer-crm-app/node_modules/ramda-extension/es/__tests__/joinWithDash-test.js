import { joinWithDash } from '../';
describe('joinWithDash', function () {
  it('join array of string with dash determiner', function () {
    expect(joinWithDash(['a', 'b', 'c'])).toEqual('a-b-c');
  });
});