import { uniqTakeLast } from '../';
describe('uniqTakeLast', function () {
  it('returns unique result of takeLast', function () {
    expect(uniqTakeLast(2, ['foo', 'baz', 'baz'])).toEqual(['baz']);
    expect(uniqTakeLast(3, ['foo', 'baz', 'baz'])).toEqual(['foo', 'baz']);
  });
});