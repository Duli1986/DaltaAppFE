import { equalsToTrue } from '../';
describe('equalsToTrue', function () {
  it('return true', function () {
    expect(equalsToTrue(true)).toBe(true);
  });
  it('return false', function () {
    expect(equalsToTrue(false)).toBe(false);
  });
});