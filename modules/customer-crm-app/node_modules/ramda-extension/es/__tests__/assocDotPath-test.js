import { assocDotPath } from '../';
describe('assocDotPath', function () {
  it('should set property on the given dot path', function () {
    expect(assocDotPath('a.b', 2, {
      a: undefined
    })).toEqual({
      a: {
        b: 2
      }
    });
  });
});