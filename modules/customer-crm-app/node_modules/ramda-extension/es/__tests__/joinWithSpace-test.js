import { joinWithSpace } from '../';
describe('joinWithSpace', function () {
  it('join array of string with space determiner', function () {
    expect(joinWithSpace(['a', 'b', 'c'])).toEqual('a b c');
  });
});