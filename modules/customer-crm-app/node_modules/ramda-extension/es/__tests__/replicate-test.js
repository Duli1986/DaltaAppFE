import { replicate } from '../';
describe('replicate', function () {
  it('it should replicate a value n times', function () {
    expect(replicate(3, 6)).toEqual([6, 6, 6]);
  });
  it('it should be curried', function () {
    expect(replicate(3)(6)).toEqual([6, 6, 6]);
  });
});