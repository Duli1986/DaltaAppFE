import { uniqTake } from '../';
describe('uniqTake', function () {
  it('returns unique result of take', function () {
    expect(uniqTake(2, ['foo', 'foo', 'baz'])).toEqual(['foo']);
    expect(uniqTake(3, ['foo', 'foo', 'baz'])).toEqual(['foo', 'baz']);
  });
});