import { identity } from 'ramda';
import { uniqMap } from '../';
describe('uniqMap', function () {
  it('returns unique values of mapping', function () {
    expect(uniqMap(identity, ['hello', 'hello', 'world'])).toEqual(['hello', 'world']);
  });
});