import { uniqTakeWhile } from '../';
describe('uniqTakeWhile', function () {
  it('returns uniq result of takeWhile', function () {
    expect(uniqTakeWhile(function (x) {
      return x !== 4;
    }, [1, 2, 2, 1, 3, 4, 3, 2, 1])).toEqual([1, 2, 3]);
  });
});