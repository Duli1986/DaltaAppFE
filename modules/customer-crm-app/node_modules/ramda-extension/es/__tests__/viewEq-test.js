import R from 'ramda';
import { viewEq } from '../';
describe('viewEq', function () {
  it('lens prop foo should be same as bar', function () {
    expect(viewEq(R.lensProp('foo'), 'bar', {
      foo: 'bar'
    })).toEqual(true);
  });
});