import { multiply, add } from 'ramda';
import { applyPipe } from '../';
describe('applyPipe', function () {
  it('should apply pipe to list of functions', function () {
    expect(applyPipe([multiply(2), add(1)])(3)).toBe(7);
  });
});