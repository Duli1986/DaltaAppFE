import { equalsToNull } from '../';
describe('equalsToNull', function () {
  it('return true', function () {
    expect(equalsToNull(undefined)).toBe(false);
  });
  it('return false', function () {
    expect(equalsToNull(null)).toBe(true);
  });
});