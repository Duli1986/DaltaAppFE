import { uniqPrepend } from '../';
describe('uniqPrepend', function () {
  it('prepends to unique list', function () {
    expect(uniqPrepend('b', ['a', 'b'])).toEqual(['b', 'a']);
    expect(uniqPrepend('d', ['a', 'b'])).toEqual(['d', 'a', 'b']);
  });
});