import { notEqualToNull } from '../';
describe('notEqualToNull', function () {
  it('returns true', function () {
    expect(notEqualToNull(0)).toEqual(true);
  });
  it('returns false', function () {
    expect(notEqualToNull(null)).toEqual(false);
  });
});