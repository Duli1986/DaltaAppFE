import { alwaysEmptyObject } from '../';
describe('alwaysEmptyObject', function () {
  it('returns {}', function () {
    expect(alwaysEmptyObject()).toEqual({});
  });
  it('always returns a new object', function () {
    expect(alwaysEmptyObject()).not.toBe(alwaysEmptyObject());
  });
});