import { equalsToEmptyArray } from '../';
describe('equalsToEmptyArray', function () {
  it('return true', function () {
    expect(equalsToEmptyArray([])).toBe(true);
  });
  it('return false', function () {
    expect(equalsToEmptyArray(['Ramda'])).toBe(false);
  });
});