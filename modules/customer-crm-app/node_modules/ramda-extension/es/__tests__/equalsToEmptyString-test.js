import { equalsToEmptyString } from '../';
describe('equalsToEmptyString', function () {
  it('return true', function () {
    expect(equalsToEmptyString('')).toBe(true);
  });
  it('return false', function () {
    expect(equalsToEmptyString('Ramda')).toBe(false);
  });
});