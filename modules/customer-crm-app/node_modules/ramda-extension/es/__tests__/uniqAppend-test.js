import { uniqAppend } from '../';
describe('uniqAppend', function () {
  it('appends unique item', function () {
    var result = uniqAppend('a', ['a', 'b']);
    expect(result).toEqual(['a', 'b']);
  });
});