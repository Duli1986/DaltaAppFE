import { uniqTakeLastWhile } from '../';
describe('uniqTakeLastWhile', function () {
  it('returns uniq result of takeLastWhile', function () {
    expect(uniqTakeLastWhile(function (x) {
      return x !== 4;
    }, [1, 2, 3, 4, 3, 2, 1, 1])).toEqual([3, 2, 1]);
  });
});