import { append } from 'ramda';
import pipeC from '../pipeC';
import rejectNil from '../rejectNil';
describe('pipeC', function () {
  it('should provide curried pipe', function () {
    var appendAndRejectNil = pipeC(append, rejectNil);
    var appendCAndRejectNil = appendAndRejectNil('c');
    expect(appendCAndRejectNil(['a', null])).toEqual(['a', 'c']);
  });
});