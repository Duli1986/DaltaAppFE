import { uniqDropLast } from '../';
describe('uniqDropLast', function () {
  it('returns uniq result of dropLast', function () {
    expect(uniqDropLast(2, ['foo', 'foo', 'foo'])).toEqual(['foo']);
  });
});