import { argumentsToList } from '../';
describe('argumentsToList', function () {
  it('converts variadic arguments into list', function () {
    expect(argumentsToList(1, 3, 4, 3)).toEqual([1, 3, 4, 3]);
  });
});