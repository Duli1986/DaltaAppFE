import { equalsToEmptyObject } from '../';
describe('equalsToEmptyObject', function () {
  it('return true', function () {
    expect(equalsToEmptyObject({})).toBe(true);
  });
  it('return false', function () {
    expect(equalsToEmptyObject({
      ramda: true
    })).toBe(false);
  });
});