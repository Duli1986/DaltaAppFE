import { joinWithDot } from '../';
describe('joinWithDot', function () {
  it('join array of string with dot determiner', function () {
    expect(joinWithDot(['a', 'b', 'c'])).toEqual('a.b.c');
  });
});