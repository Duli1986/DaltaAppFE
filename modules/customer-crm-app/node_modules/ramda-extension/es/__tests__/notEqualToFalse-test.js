import { notEqualToFalse } from '../';
describe('notEqualToFalse', function () {
  it('returns true', function () {
    expect(notEqualToFalse(true)).toEqual(true);
  });
  it('returns false', function () {
    expect(notEqualToFalse(false)).toEqual(false);
  });
});