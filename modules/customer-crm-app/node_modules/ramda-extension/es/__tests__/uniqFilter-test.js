import { uniqFilter, equalsLength } from '../';
describe('uniqFilter', function () {
  it('returns unique result of filter', function () {
    expect(uniqFilter(equalsLength(5), ['h', 'hello', 'hello', 'world'])).toEqual(['hello', 'world']);
  });
});