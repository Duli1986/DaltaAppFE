import R from 'ramda';
import { overHead } from '../';
describe('overHead', function () {
  it('should remap first index of array', function () {
    expect(overHead(R.toUpper, ['foo', 'bar', 'baz'])).toEqual(['FOO', 'bar', 'baz']);
  });
});