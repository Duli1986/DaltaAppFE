import { notInclude } from '../';
describe('notInclude', function () {
  it('returns false if array contains item', function () {
    expect(notInclude(['a'], 'a')).toEqual(false);
  });
  it('returns true if array does not contain item', function () {
    expect(notInclude(['a'], 'b')).toEqual(true);
  });
});