import { uniqLength } from '../';
describe('uniqLength', function () {
  it('returns length of unique values', function () {
    expect(uniqLength).toBeTruthy();
    expect(uniqLength(['hello', 'world', 'world'])).toEqual(2);
  });
});