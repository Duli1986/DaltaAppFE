import { mapIndexed } from '../';
describe('mapIndexed', function () {
  it('should exist', function () {
    expect(mapIndexed(function (value, index) {
      return "".concat(value, "-").concat(index);
    })([1, 2, 3])).toEqual(['1-0', '2-1', '3-2']);
  });
});