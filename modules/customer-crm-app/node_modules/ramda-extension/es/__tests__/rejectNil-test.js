import { rejectNil } from '../';
describe('rejectNil', function () {
  it('should be defined', function () {
    expect(rejectNil).toBeDefined();
  });
  it('should filter nill values', function () {
    expect(rejectNil(['foo', null, 'foo', undefined])).toEqual(['foo', 'foo']);
  });
});