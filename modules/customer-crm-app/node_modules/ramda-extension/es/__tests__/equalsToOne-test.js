import { equalsToOne } from '../';
describe('equalsToOne', function () {
  it('should be false', function () {
    expect(equalsToOne(0)).toBe(false);
  });
  it('should be true', function () {
    expect(equalsToOne(1)).toBe(true);
  });
});