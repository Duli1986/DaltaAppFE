import { uniqDropWhile } from '../';
describe('uniqDropWhile', function () {
  it('returns unique result of dropWhile', function () {
    expect(uniqDropWhile(function (x) {
      return x !== 4;
    }, [1, 2, 2, 1, 3, 4, 3, 2, 1])).toEqual([4, 3, 2, 1]);
  });
});